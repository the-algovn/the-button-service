# the-button plan skeleton — FIXED INTERFACES (authors: do not deviate)

Spec (read first): /Users/duclm27/the-algovn/the-button-service/docs/superpowers/specs/2026-07-14-the-button-design.md

## Global constraints (verbatim into every cluster's thinking)
- Go 1.26.4. Modules: `github.com/the-algovn/the-button-service`. Images amd64-only, ghcr.io, CI = the api-control-plane repo's .github/workflows/build.yaml pattern (test job gates build; semver+sha+main tags).
- iac: SealedSecrets only (kubeseal --context algovn-remote --controller-name sealed-secrets --controller-namespace sealed-secrets); ./scripts/validate.sh before every push; compact flow-map YAML style; Services carry `labels: { app: <name> }`; VMServiceScrape in ns monitoring; Argo Application per app in clusters/algovn/{apps,platform}/, sync-wave "1"; ImageUpdater CRs modeled on platform/image-updater/api-control-plane-updater.yaml; app namespaces get sealed registry-creds (copy pattern from apps/api-control-plane) because GHCR packages are PRIVATE.
- Commits: imperative subject, small, NO Co-Authored-By/Generated-with trailers. Repo publish steps (gh repo create --public) are USER-GATED: plan step must say "controller asks user, then runs".
- testcontainers via podman: export DOCKER_HOST="unix://$(podman machine inspect --format '{{.ConnectionInfo.PodmanSocket.Path}}')"; export TESTCONTAINERS_RYUK_DISABLED=true; integration tests build-tagged `//go:build integration`.
- testify require; TDD (RED/GREEN evidence) for every Go/TS code task. SPA tests: vitest.

## Cross-cluster interface freeze
- Proto: package `algovn.button.v1`, go_package `github.com/the-algovn/protos/gen/go/algovn/button/v1;buttonv1`. RPCs/messages EXACTLY as spec §4. Tag: `gen/go/v0.2.0`.
- Service: ns `the-button`, svc `the-button-service` headless :9090 grpc / :9091 metrics, image `ghcr.io/the-algovn/the-button-service:main`, replicas 2.
- Service env: `PG_URL` (from CNPG-generated secret `pg-the-button` key `uri`... actual key per iac/docs/runbooks/postgres.md declarative onboarding — author D verifies), `REDIS_URL` (redis://:<pass>@redis.redis.svc.cluster.local:6379/0, sealed secret `redis-creds` key `url` in ns the-button), `AMQP_URL` (sealed `amqp-creds` key `url`, same as demo-service), `POW_SECRET` (sealed `pow-secret` key `key`, 32 random bytes b64), `POW_W0` (default "16384"), `LISTEN_ADDR` :9090, `METRICS_ADDR` :9091.
- Postgres: database `the_button`, owner role `the_button`, declarative via postgres.md; schema (spec §7): user_clicks, user_achievements; DDL applied by service at startup (idempotent CREATE TABLE IF NOT EXISTS).
- Redis platform: ns `redis`, svc `redis.redis.svc.cluster.local:6379`, image docker.io/redis:7.4-alpine, args --requirepass from sealed secret `redis-auth` key `password`, --appendonly yes --appendfsync everysec, PVC 2Gi local-path, redis_exporter sidecar (oliver006/redis_exporter) :9121 metrics, VMServiceScrape. Password manager entry name: `redis-algovn`.
- Redis keys (spec §7): pow:<uuid> EX 330 · throttle:<sub> EX 2-10 · counter:global · milestone:<threshold> · pow:L · pow:min_interval · stats:accepted_total.
- Go deps: github.com/redis/go-redis/v9, github.com/jackc/pgx/v5/pgxpool, github.com/rabbitmq/amqp091-go, google.golang.org/grpc+protobuf, github.com/google/uuid (UUIDv7 via uuid.NewV7), testify. NO ORM, NO migration framework.
- PoW token: base64url(payloadJSON || HMAC-SHA256(payloadJSON, K)); payload {"id","sub","iat","exp","w0","l","min_interval_s","max_batch"}; hash check SHA-256(tokenBytes || be32(count) || be64(nonce)) < 2^256/(w0*count*l). Difficulty: L 1..16, hysteresis raise>110%/lower<70% of band [200,400] accepted/s, slew ≤1 step per 30s; min_interval ladder 2/5/10s. TTL 300s (+30s verify leeway, Redis EX 330).
- acp changes (repo /Users/duclm27/the-algovn/api-control-plane): HTTPStatus adds ResourceExhausted→429, AlreadyExists→409, FailedPrecondition→400 (internal/transcode/invoke.go); handleAPI sets `Retry-After: 2` header when 429; handleSSE writes `retry: 3000\n\n` + jitter note before first flush AND enforces global cap env `SSE_MAX_CONNS` default 15000 → 503; main.go CORS default gains `,https://algovn.com`.
- Ingress split (iac apps/api-control-plane): 3 Ingresses same host api.algovn.com — `api-events` path /events (KongPlugin rl-events second:50 minute:1000), `api-the-button` path /the-button (KongPlugin rl-button second:20 minute:600), existing `api-control-plane` path / keeps api-rate-limit 10/300. All limit_by:ip (real after T1).
- SPA: web repo `apps/the-button`, name `the-button`, Vite+React19+TS, base '/the-button/', deps: @algovn/ui workspace, oidc-client-ts, hash-wasm. Env at build: VITE_OIDC_CLIENT_ID (from Zitadel onboarding), VITE_OIDC_AUTHORITY=https://id.algovn.com, VITE_API_BASE=https://api.algovn.com/the-button, VITE_EVENTS_URL=https://api.algovn.com/events/the-button.counter. Image `ghcr.io/the-algovn/web-the-button:main` (nginx:1.27-alpine, assets Cache-Control immutable, index.html no-cache). Deploy ns `the-button-web`, Ingress host algovn.com path /the-button (class kong).
- k6 scripts in the-button-service/load/: sse-ramp.js (10k VUs SSE), click-soak.js (PoW-solving JS miner at low w0 against a STAGING difficulty override, or pre-solved token corpus — author D picks and specifies fully), rollout-drill.js.
- Zitadel: project `the-button`, SPA app PKCE, Access Token Type JWT, redirects https://algovn.com/the-button/callback + http://localhost:5173/the-button/callback. USER console task with exact click-path per iac/docs/runbooks/zitadel.md.

## Task numbering (fixed)
Phase P (platform): T1 Kong real-IP+netpol · T2 Redis component · T3 acp code changes · T4 edge scaling iac (ingress split, acp/cloudflared/Kong bumps) · T5 Zitadel bumps + w1 sysctls
Phase S (service): T6 protos v1+tag · T7 service scaffold+storage (PG DDL, pgx, go-redis wiring, config) · T8 pow package (token/verify/controller math) · T9 SubmitClicks core (burn/throttle/txn/achievements/compensation) · T10 remaining RPCs + tick leader + gRPC assembly + main · T11 Dockerfile+CI+publish(user-gated)+image
Phase W (SPA): T12 Vite scaffold in monorepo · T13 auth+API client · T14 counter view+SSE wrapper · T15 clicker+solver worker+calibration bench · T16 achievements UI · T17 nginx image+web CI
Phase D (deploy/verify): T18 iac deploy service+SPA+registration+secrets+PG DB · T19 Zitadel onboarding (USER) + e2e smoke · T20 pg_test_fsync+soak+solver calibration→w0 · T21 k6 suite runs · T22 alerts+runbook+catalog status flip

Authors: write ONLY your assigned tasks, in the exact writing-plans task format (Files/Interfaces/checkbox steps with complete code, run commands with expected output, commit steps). Consumes/Produces must reference the frozen names above.
